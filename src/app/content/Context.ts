/**
 * This class will later on represent the
 * context of an activity so its state can be stored
 * in local-storage or in a database if necessary.
 *
 * @author David Riedl <daves.weblab@gmail.com>
 */
export abstract class Context {
    // todo
}